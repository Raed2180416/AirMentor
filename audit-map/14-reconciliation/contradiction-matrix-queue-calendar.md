# Contradiction Matrix: Queue / Calendar / HOD

| ID | Topic | Expected | Observed | Evidence location | Next action |
| --- | --- | --- | --- | --- | --- |
| QC-001 | concernContextKey spec presence | `concernContextKey` field should identify the primary concern context per auth-prompt spec (frozen appendix) | No `concernContextKey` field found anywhere in repo (frontend or backend). Governance detail JSON uses `queueCaseId`, `primaryCase`, `countsTowardCapacity`, `governanceReason` instead. | `air-mentor-api/src/lib/proof-queue-governance.ts:49-64`, `air-mentor-api/src/lib/proof-control-plane-hod-service.ts:372-380` | Open: spec field has no code counterpart; doc must align or code must add the field |
| QC-002 | Primary vs workflow case taxonomy | Primary student concern cases (`countsTowardCapacity=true`, `primaryCase=true`) must be distinguished from workflow tasks (`SharedTask`) in UI and governance | Code separates these correctly: `reassessmentEvents`/`simulationStageQueueProjections` carry `primaryCase` + `countsTowardCapacity` in `detailJson`; `SharedTask` in domain is a different type system with `scheduleMeta`, `dismissal`, `transitionHistory` | `air-mentor-api/src/lib/proof-queue-governance.ts:55`, `air-mentor-api/src/lib/proof-control-plane-hod-service.ts:382-384`, `src/domain.ts:205-211`, `src/domain.ts:416-418` | Resolved locally; ensure queue views never surface workflow tasks as primary cases |
| QC-003 | Ownership routing (High → Mentor, Medium → CL) | High-risk → Mentor; Medium-risk → Course Leader; HOD sees both via scope | Monitoring engine assigns exactly `Mentor` for High, `Course Leader` for Medium; HOD scope via `roleGrants` row `roleCode='HOD'` at `scopeType=department/branch`; `ProofQueueRole` type enforces the three roles | `air-mentor-api/src/lib/monitoring-engine.ts:40-65`, `air-mentor-api/src/lib/proof-queue-governance.ts:17`, `air-mentor-api/src/lib/proof-control-plane-hod-service.ts:217-225` | Resolved; no contradiction found |
| QC-004 | dismissal=handled semantics | Docs may reference `dismissal=handled` as a status value | Code uses `!!task.dismissal` boolean (not an enum value "handled"). `TaskDismissalState.kind` is `'task' | 'series'`. Restore window is 60 days (`dismissedAt + 60*24*60*60*1000`). No "handled" string enum exists anywhere. | `src/domain.ts:205-212`, `src/domain.ts:416-418`, `air-mentor-api/src/modules/academic-runtime-routes.ts:269`, `src/App.tsx:2372-2376` | Open: doc language "dismissal=handled" does not match code — code uses presence of `dismissal` object, not a status string |
| QC-005 | reopen-later-deterioration gate | After a resolved case is reopened and risk deteriorates, a deterioration gate should escalate | Reopen adds `Reopened` to `transitionHistory` (App.tsx:2424). No subsequent deterioration gate exists — next alert/watch decision is purely re-derived from the next evidence window via `buildMonitoringDecision`. No special escalation path for post-reopen deterioration. | `src/App.tsx:2422-2425`, `air-mentor-api/src/lib/monitoring-engine.ts:25-75` | Open: spec claims deterioration→escalation after reopen; code has no such path |
| QC-006 | queue↔calendar bridge | Queue task `dueAt` / `dueDateISO` should be updatable via calendar drag; audit trail should record event | `resolveTimedHoverRange` and `clampRangeToDayBounds` produce normalized date; `dueDateISO: normalizedDate` is written on task (calendar-utils.ts:658). Audit event `'task-rescheduled'` emitted (domain.ts:219). | `src/calendar-utils.ts:646-658`, `src/domain.ts:217-222`, `src/pages/calendar-pages.tsx:1331`, `src/App.tsx:2447-2485` | Resolved; bridge is implemented correctly |
| QC-007 | drag→due-date write | Dragging a task in week/month calendar view should update the task's `dueDateISO` | Confirmed: drag sets `dueDateISO: normalizedDate` (calendar-utils.ts:658). Drag threshold=4px, snap=14 min (calendar-pages.tsx:47-48). Placement mode toggles between `'timed'` and `'untimed'`. | `src/pages/calendar-pages.tsx:47-48`, `src/calendar-utils.ts:646-682`, `src/App.tsx:2447-2485` | Resolved; no contradiction |
| QC-008 | demo auto-resolution | Auth-prompt may describe a demo mode that auto-resolves queue cases | No demo flag, no auto-resolution trigger found in codebase. Cases auto-resolve only via governance: `post-see` watchlist → `resolved` (proof-queue-governance.ts:344) or `no_longer_actionable` (proof-queue-governance.ts:355-360). No separate demo-triggered path. | `air-mentor-api/src/lib/proof-queue-governance.ts:337-370` | Open: if auth-prompt claims demo auto-resolution, it has no code counterpart |
| QC-009 | HOD correction cycle — clear-lock step | HOD should be able to clear a lock on an assessment kind after an unlock request is approved | Route `POST /api/academic/offerings/:offeringId/assessment-entries/:kind/clear-lock` exists, HOD-only, clears `tt1Locked/tt2Locked/quizLocked/assignmentLocked/finalsLocked = 0`, updates `lockByOffering` runtime state, emits `CLEAR_LOCK` audit event. | `air-mentor-api/src/modules/academic-runtime-routes.ts:1285-1331` | Resolved; clear-lock step is implemented |
| QC-010 | HOD correction cycle — request phase | The full cycle request→approve→reset-unlock→edit→recompute→relock should be traceable in code and seeds | Seed data shows HOD unlock request payload with `requestNote` and `actionHint` for TT1 late-moderation correction (seed:94169-94237). Clear-lock route covers reset-unlock. Edit covered by `PUT /api/academic/offerings/:offeringId/assessment-entries/:kind/commit` (lock:true). Recompute via `enqueueProofSimulationRun`. Relock by re-committing with `locked:true`. | `air-mentor-api/src/db/seeds/platform.seed.json:94169`, `air-mentor-api/src/modules/academic-runtime-routes.ts:1285-1331`, `air-mentor-api/src/lib/proof-run-queue.ts:131-211` | Needs-doc-update: full cycle is traceable but no single doc captures all 5 steps end-to-end |
| QC-011 | Queue capacity admission gate | Section capacity rate limit enforced before case admitted to actionable queue | `proofQueueActionableRateLimitForStage` returns 0.30 (default) or 0.35 (post-see). `sectionLimit = floor(sectionStudentCount * rate)`. Cases exceeding limit get `watch` status with reason `open_candidate_pruned_by_caps`. | `air-mentor-api/src/lib/proof-queue-governance.ts:82-86`, `air-mentor-api/src/lib/proof-queue-governance.ts:309-319` | Resolved; gate is implemented correctly |
| QC-012 | HoD checkpoint fallback provenance | HoD response must use checkpoint semester, not active run semester, to avoid provenance drift in playback mode | Code explicitly comments and enforces this: `checkpoint.semesterNumber ?? activeRun.activeOperationalSemester ?? activeBatch.currentSemester` with inline note explaining drift risk. | `air-mentor-api/src/lib/proof-control-plane-hod-service.ts:423-434` | Resolved; provenance anchor is correct (see also C-021 in main matrix) |
| QC-013 | `queueDecisionTypeFromStatus` mapping | Status `Resolved` → `suppress`, `Watching` → `watch`, else → `alert` | Confirmed in `queueDecisionTypeFromStatus`: exactly these three branches. Used in both HoD and tail service. | `air-mentor-api/src/lib/proof-control-plane-access.ts:23-27`, `air-mentor-api/src/lib/proof-control-plane-hod-service.ts:671` | Resolved |

## Open items requiring follow-up

- QC-001: `concernContextKey` — spec field absent from all code; needs reconciliation with frozen appendix
- QC-004: `dismissal=handled` — doc language mismatch with code semantics
- QC-005: reopen-later-deterioration — no gate in code; spec claim needs withdrawal or implementation
- QC-008: demo auto-resolution — no code path; spec claim needs withdrawal or implementation
