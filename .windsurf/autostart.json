{
  "name": "AirMentor Live Watcher Autostart",
  "description": "Auto-start deterministic codebase watcher on project open",
  "commands": [
    {
      "command": "systemctl --user is-active airmentor-live-watcher || systemctl --user start airmentor-live-watcher",
      "description": "Ensure live watcher is running"
    },
    {
      "command": "node scripts/deterministic-codebase-indexer.mjs",
      "description": "Force regenerate deterministic index on open"
    }
  ]
}
