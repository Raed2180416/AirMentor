# Dry-run pass A

Synthetic pass used to prove the pipeline end-to-end without any LLM.
The dry-run agent stub parses this bundle, writes the required artifact, then
emits the structured exit marker. Do not use in production.
